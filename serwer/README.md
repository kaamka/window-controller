# Server part of Smart Window Controller

## Running locally without docker

Install requirements:
```
pip3 install -r requirements.txt
```

To run the API:
```
python3 app.py
```

To run the data collection loop
```
python3 ping_arduino.py
```